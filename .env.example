# Web app environment (server-side only; never exposed to the browser).
# The single fixed backend address the /api proxy forwards to. When it is unset
# the app shows an explicit "backend not configured" state.
API_INTERNAL_URL=http://127.0.0.1:3001
