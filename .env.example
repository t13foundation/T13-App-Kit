# Public browser configuration. Use only a publishable key or local legacy anon key.
# Leave both empty to browse the introduction without the Notes flow.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional isolated legacy backend. Not required for Supabase Notes.
# Server-only; never VITE-prefixed.
# API_INTERNAL_URL=http://127.0.0.1:3001
