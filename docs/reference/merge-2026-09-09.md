# Accounts increment: reconciliation, 2026-09-09

Integrated `codex/app-kit-core` (`c5e422a832813dd45c6313d81e8e401d117d95de`)
with main (`89a27d874e578ef7db8034c65063308d2ec53cfa`). The merge retains both
histories, not a force-push or a wholesale restoration of an older snapshot.

Kept from main: the official Better Auth client, `/account` and `/catalog`
routing, current dependency lockfiles, complete database/mail implementation,
all applied migrations, test database guards, existing integration tests,
`.env` exclusions, docs/module structure and manual-only CI.

Integrated from the branch: actual profile editing, caller-scoped account
export, individual session revocation, freshness/origin rules, a testable
bounded proxy, neutral Untitled UI styling, accessible root navigation and
safe error states. The account screen uses one canonical block, not two
competing implementations. All forms carrying credentials use POST.

Reconciled explicitly: current Better Auth source IP configuration is retained;
resolved peer IP is used consistently for both limiter layers. API readiness
still checks PostgreSQL. Sensitive mutations cannot use public library routes
to bypass freshness checks. Account export is not advertised as a full data
lifecycle or export of future modules. SMTP, proxy hosting and native mobile
are not silently configured by the merge.

Current verification and limits: `../status.md`.
