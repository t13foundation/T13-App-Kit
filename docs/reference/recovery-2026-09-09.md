# Recovery of the initial increment

2026-09-09. GitHub snapshot `ccd6ac2d706327e348f34ad0bb9041d0fbb4d2a1` retains the backend, migrations, tests, vendored MIT components and proxy. The later Lovable synchronization commit `a326ae7271fdb946938844f4934591c951831e0e` removed many of these files. They were NOT irretrievably lost and must not be reimplemented from memory.

Recovery restores the missing Git objects while retaining the subsequent screens, official auth client, corrected contracts, neutral theme and reviewed server app/auth/env. The later UI additions in `ca75771644c98ae08b1f05ef27e87369c22be496` are retained. This is a forward commit, not history rewriting. No database was altered.

`server/.env.test` is deliberately not restored. It contained development-only settings; generate test secrets locally. Environment files are ignored, and AUTH_URL points to the public web origin in the example.

Source recovery is not an integration test. Before running restored integration tests, implement the isolated local appkit_test database guard. Regenerate the route tree, run checks/build/tests with real exit codes, and update docs/status.md. Do not rely on a report for an earlier or incomplete snapshot.
